% Matlab Script for Generating Data for Simulation

num_samples = 1000;
runHot = zeros(num_samples, 1);
setActive = [25:250];
runHot(setActive) = 1;

vCar = 25* ones(num_samples, 1);


lidarData = 100 * (num_samples, 10);

